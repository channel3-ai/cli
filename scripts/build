#!/usr/bin/env bash

set -euo pipefail

cd "$(dirname "$0")/.."

# Mark the necessary Go modules as private to avoid Go's proxy
export GOPRIVATE="${GOPRIVATE:+$GOPRIVATE,}github.com/channel3-ai/sdk-go,github.com/stainless-sdks/public-sdk-go"

echo "==> Building channel3"
go build ./cmd/channel3
